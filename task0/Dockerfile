# Use the latest Ubuntu base image
FROM ubuntu:latest

# Update APT and upgrade installed packages
RUN apt-get update && apt-get upgrade -y

# Set the command to output "Hello, World!"
CMD ["echo", "Hello, World!"]
